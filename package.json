{
  "name": "qr-code-scan-computer-visionj",
  "private": true,
  "version": "2.0.0",
  "description": "Smart Fitao phone photo for 2D try-on — QR scan, simple camera, sync to main website",
  "scripts": {
    "dev": "vercel dev",
    "build": "node -e \"console.log('static phone camera app — no build step')\""
  },
  "dependencies": {
    "@vercel/blob": "^0.27.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
